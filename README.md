# CECS 453 Lab 7 - Notes App

This Flutter app allows the user to create, update, delete, and persist notes. Each note displays id, title, description, and created date. Notes are saved using SharedPreferences and restored when the app is relaunched.

Run:

```bash
flutter pub get
flutter run
flutter run -d chrome --web-port=8080
```
